# 销售预测助手 (Sales Forecast Helper)

自动分析历史销售数据，按 ABC 分类法为每个产品生成未来 4 个月的销量预测推荐值。

---

## ✨ 功能特点

- **智能分类**：自动将产品分为 A(稳定) / B(波动) / C(稀疏) 三类
- **分层算法**：针对不同产品类型使用最适合的预测模型
- **Open SO 检查**：自动校验 8 月预测是否覆盖在手订单
- **零依赖烦恼**：一键运行，输出标准 Excel 表格

---

## 📦 安装

### 方式一：直接运行（推荐）

```bash
# 1. 克隆仓库
git clone https://github.com/YOUR_USERNAME/sales-forecast-helper.git
cd sales-forecast-helper

# 2. 安装依赖
pip install -r requirements.txt

# 3. 运行
python forecast.py -i "你的Excel文件路径.xlsx" -s "ROCKY JIANG"
```

### 方式二：Windows 双击运行

1. 安装 [Python](https://www.python.org/downloads/)（勾选 "Add to PATH"）
2. 安装依赖：`pip install pandas openpyxl numpy`
3. 双击 `run.bat` 或在命令行运行

---

## 🚀 快速开始

### 命令行方式

```bash
python forecast.py -i "C:\Users\xxx\Downloads\销售预测表.xlsx" -s "ROCKY JIANG"
```

### 参数说明

| 参数 | 说明 | 必填 |
|------|------|------|
| `-i, --input` | 输入的 Excel 文件路径 | ✅ |
| `-s, --salesperson` | 销售员英文名（须与 Excel 中一致） | ✅ |
| `-o, --output` | 输出文件路径（可选，默认自动生成） | ❌ |

### 输出示例

```
======================================================================
           销售预测助手 v1.0
======================================================================

📂 读取文件: 2026经销商预测.xlsx
📊 使用Sheet: 销售预测收集26年8-11月
   总行数: 1023

👤 销售员: ROCKY JIANG
   产品数: 81

📈 预测结果统计:
   A类(稳定): 31 个
   B类(波动): 18 个
   C类(稀疏): 32 个
   4个月预测总量: 17,737,796 M2

✅ 结果已保存: 预测结果_ROCKY_JIANG_0810_1940.xlsx
```

---

## 📊 ABC 分类逻辑

| 类型 | 判定标准 | 预测方法 |
|------|---------|---------|
| **A - 稳定产品** | 数据≥8月，CV<0.8，平均≥5000 | 加权移动平均 + 趋势修正 |
| **B - 波动/季节性** | 数据≥6月，高波动或季节性明显 | 中位数法 + 峰值/谷底控制 |
| **C - 稀疏/微量** | 数据<6月 或 平均<3000 | 保守估计，避免虚高 |

> **CV（变异系数）** = 标准差 / 平均值，衡量数据波动程度

---

## 🧮 三种预测算法

### A 类 - 稳定产品

```
预测 = 近期3月MA × 0.40 + 去年同期 × 0.30 + 全年平均 × 0.20 + 趋势 × 0.10
```

8 月特殊处理：若 Open SO > 0，预测 ≥ Open SO × 1.05

### B 类 - 波动产品

```
预测 = 历史中位数 × 0.35 + 近期平均 × 0.35 + min(去年同期, P75) × 0.30
约束：P20 × 0.8 ≤ 预测 ≤ P80 × 1.05
```

### C 类 - 稀疏产品

```
若 历史平均 < 1000：预测 = 0
若 8月有 Open SO：预测 = Open SO
其他：预测 = 近期平均 × 0.5
```

---

## 📁 项目结构

```
sales-forecast-helper/
├── forecast.py          # 主程序
├── run.bat              # Windows 一键运行脚本
├── requirements.txt     # Python 依赖
├── README.md            # 本文件
├── LICENSE              # MIT 许可证
└── .gitignore           # Git 忽略配置
```

---

## ⚠️ 注意事项

1. **Open SO 规则**：8 月预测值(W) **必须 ≥** Open SO(V)。程序会自动检查并警告。

2. **算法 vs 业务判断**：算法基于纯历史数据，看不到你知道的信息（如客户已确认大单、促销计划等）。**有业务依据时，优先信你的判断**；如果只是"感觉"，建议参考算法。

3. **Excel 格式要求**：
   - 第 4 列(索引3) = 销售员
   - 第 5-16 列(索引5-16) = 12 个月历史数据
   - 第 22 列(索引21) = Open SO
   - 包含 "销售预测收集" 的 Sheet 名

---

## 🔧 常见问题

**Q: 提示 "找不到文件"？**
> 检查文件路径是否正确，建议使用绝对路径，Windows 路径使用双反斜杠 `\\` 或加 `r` 前缀。

**Q: 提示 "找不到销售员"？**
> 确认销售员英文名拼写与 Excel 中完全一致（区分大小写、空格）。

**Q: 公司换了 Excel 格式怎么办？**
> 修改 `forecast.py` 中列的索引号，或提交 Issue 寻求帮助。

**Q: 能不能做成 Excel VBA 版本？**
> 目前暂未提供，欢迎提交 PR！

---

## 📜 License

[MIT License](LICENSE)

---

## 🙏 致谢

本项目为销售团队提供数据驱动的预测辅助，帮助减少盲目照搬系统建议带来的库存风险。
